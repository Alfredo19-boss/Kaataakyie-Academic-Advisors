{
  "name": "northbound-advising",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Client-progress platform for US master's admissions advising — advisor console, isolated client portals, 426-school directory and a resource library.",
  "license": "MIT",
  "engines": { "node": ">=18" },
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs && node dev/serve.mjs",
    "check": "node --check src/data/schools.js && node --check src/data/content.js && node --check src/app/core.js && node --check src/app/views.js && node --check dev/harness.js && echo \"syntax OK\""
  },
  "keywords": ["education", "admissions", "advising", "dashboard", "student-portal"]
}
